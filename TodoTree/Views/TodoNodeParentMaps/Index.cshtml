@model IEnumerable<TodoModels.Models.TodoNodeParentMap>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ParentNode)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.TodoNode)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ParentNode.Title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.TodoNode.Title)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TodoNodeParentMapId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TodoNodeParentMapId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TodoNodeParentMapId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
